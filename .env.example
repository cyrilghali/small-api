# Server Configuration
PORT=3000

# Security: Signature Secret
# REQUIRED: Must be at least 32 characters
# Generate with: openssl rand -base64 32
SIGNATURE_SECRET=your-secret-key-at-least-32-characters-long

# Performance & Environment
# NODE_ENV: Set to 'production' for optimizations
NODE_ENV=development

# Logging
# DISABLE_LOGGING: Set to 'true' to skip logger middleware (useful for load tests)
# DISABLE_LOGGING=true
